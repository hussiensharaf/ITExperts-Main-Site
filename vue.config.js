module.exports = {
  transpileDependencies: [
    'vuetify'
  ],
  publicPath: './',
  // Optional: place compiled assets in a 'static' folder
  outputDir: 'dist',
  assetsDir: 'static',

  // Optional: remove source maps in production for smaller build
  productionSourceMap: false
}
